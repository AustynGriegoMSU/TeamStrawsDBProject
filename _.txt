def employee_menu():
    db = BankingDB()
    db.setup_tables()
    db.add_sample_accounts()
 
    print("=== Employee Branch View: Deposit / Withdrawal Management ===")
 
    employee_name = input("Enter employee name: ").strip()
    branch_name = input("Enter branch name: ").strip()
 
    while True:
        print("\n--- Account Lookup ---")
        print("Enter an account ID to manage an account")
        print("Or type 0 to exit")
 
        try:
            account_id = int(input("Enter account ID: "))
        except ValueError:
            print("Invalid account ID.")
            continue
 
        if account_id == 0:
            print("Exiting employee branch view.")
            break
 
        account = db.get_account(account_id)
 
        if not account:
            print("Account not found.")
            continue
 
        while True:
            print("\n==============================")
            db.show_account(account_id)
            db.show_transactions(account_id)
            print("==============================")
 
            print("\nTransaction Menu")
            print("1. Deposit")
            print("2. Withdraw")
            print("3. Choose another account")
            print("4. Exit")
 
            choice = input("Enter choice: ").strip()
 
            if choice == "1":
                try:
                    amount = float(input("Enter deposit amount: "))
                    db.deposit(account_id, amount, employee_name, branch_name)
                except ValueError:
                    print("Invalid amount.")
 
            elif choice == "2":
                try:
                    amount = float(input("Enter withdrawal amount: "))
                    db.withdraw(account_id, amount, employee_name, branch_name)
                except ValueError:
                    print("Invalid amount.")
 
            elif choice == "3":
                break
 
            elif choice == "4":
                print("Exiting employee branch view.")
                return
 
            else:
                print("Invalid option. Try again.")